import React from 'react';
import styles from './footer.module.css'

const Footer = () => {
    return (
        <footer>
            <h6 className={styles.footer}>Hello World</h6>
        </footer>
    );
};

export default Footer;